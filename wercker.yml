box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.15
        theme: nofancy
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        basedir: public
